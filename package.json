{
	"name": "marketplace",
	"version": "1.0.0",
	"description": "",
	"main": "backend/src/server.ts",
	"scripts": {
		"start": "cd backend && ts-node src/server.ts",
		"test": "cd backend && ts-mocha src/tests",
		"dev": "concurrently --kill-others \"cd frontend && yarn tailwindcss -i view/input.css -o dist/output.css --watch\" \"cd frontend && yarn webpack\" \"cd backend && nodemon src/server.ts\""
	},
	"license": "ISC",
	"author": "daniel <danielnyong66@gmail.com>",
	"repository": "https://github.com/Dan6200/Marketplace.git",
	"devDependencies": {
		"concurrently": "^7.3.0"
	}
}
