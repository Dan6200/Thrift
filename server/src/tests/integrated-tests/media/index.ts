import { token } from 'morgan'
import { productMedia } from '../data/users/vendors/user-aliyu/index.js'
import { testUploadProductMedia } from '../helper-functions/products/index.js'

// it("it should add the product's media to an existing product", async () => {
// 	for (const [storeId, productIds] of storeIds.entries()) {
// 		for (const productId of productIds!)
// 			await testUploadProductMedia(server, token, mediaRoute, productMedia, {
// 				store_id: storeId,
// 				product_id: productId,
// 			})
// 	}
// })
